---
- name: Example Playbook
  hosts: all
  tasks:
    - name: "Include ansible-bootstrap-python"
      ansible.builtin.include_role:
        name: "ansible-bootstrap-python"

    - name: "Include ansible-influxdb"
      ansible.builtin.include_role:
        name: "ansible-influxdb"
